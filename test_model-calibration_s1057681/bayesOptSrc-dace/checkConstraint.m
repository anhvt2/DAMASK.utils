function a = checkConstraint(x)
	% return 
	% 	1 if no constraint is violated
	%	0 if any constraint is violated
	%% TO BE DEPRECATED and replaced by a checkConstraint.m in ${modelName}_Template/
	a = 1;
	return
end
